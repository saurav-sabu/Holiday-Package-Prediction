from setuptools import setup, find_packages

# Setup function to handle package metadata and configuration
setup(
    name="Holiday-Package-Prediction",
    version="0.0.0",
    author="Saurav Sabu",
    author_email="saurav.sabu9@gmail.com",
    packages=find_packages()  # Automatically find and include all packages in the directory
)